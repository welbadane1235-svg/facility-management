(function(){
  'use strict';
  const VERSION='V435 النظام الموحد';
  const $=id=>document.getElementById(id);
  const S=v=>String(v??'').trim();
  const N=v=>Number(String(v??'').replace(/,/g,''))||0;
  const esc=s=>S(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const money=v=>N(v).toLocaleString('en-US',{minimumFractionDigits:0,maximumFractionDigits:2});
  const today=()=>{const d=new Date();return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0')};
  const monthStart=m=>m+'-01';
  const daysInMonth=m=>{const [y,mo]=m.split('-').map(Number); return new Date(y,mo,0).getDate()||30};
  const monthEnd=m=>{const [y,mo]=m.split('-').map(Number); const d=new Date(y,mo,0); return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0')};
  const norm=s=>S(s).toLowerCase().replace(/[إأآا]/g,'ا').replace(/[ىي]/g,'ي').replace(/ة/g,'ه').replace(/[ًٌٍَُِّْـ]/g,'').replace(/\s+/g,' ').trim();
  const isPresent=s=>['present','حاضر','حضور','ح'].includes(norm(s));
  const isAbsent=s=>['absent','غائب','غياب','غ'].includes(norm(s));
  let st={workers:[],dist:[],attendance:[],projects:[],saved:[],rows:[]};
  function msg(t,bad){let el=$('salaryMsg'); if(el){el.textContent=t; el.className='msg '+(bad?'err':''); el.classList.remove('hidden'); setTimeout(()=>el.classList.add('hidden'),6500);} console[bad?'warn':'log'](t)}
  async function all(table,select='*',build){let out=[],from=0,size=1000; while(true){let q=window.sb.from(table).select(select).range(from,from+size-1); if(build) q=build(q); const r=await q; if(r.error) throw r.error; out=out.concat(r.data||[]); if(!r.data||r.data.length<size) break; from+=size;} return out;}
  async function tryAll(table,select='*',build){try{return await all(table,select,build)}catch(e){console.warn('salary load skip',table,e.message);return[]}}
  function codeOf(w){return S(w.employee_code||w.code||w.employee_ts_id||w.id)}
  function nameOf(w){return S(w.app_name||w.employee_name||w.name||w.full_name||w.worker_name||w.residency_name)}
  function resName(w){return S(w.residency_name||w.iqama_name||w.name_on_iqama||w.employee_residency_name||w.employee_name||w.name||w.full_name)}
  function iqamaOf(w){return S(w.iqama_number||w.iqama_no||w.residency_number||w.id_number)}
  function jobOf(w){return S(w.job_title||w.position||w.role_name||'عامل')}
  function allowanceOf(w){return N(w.allowance ?? w.allowances ?? w.salary_allowance ?? w.allowance_amount ?? w.housing_allowance ?? 0)}
  function basicOf(w){return N(w.basic_salary ?? w.salary ?? w.base_salary ?? 0)}
  function activeWorker(w){return !['deleted','inactive','stopped','محذوف','موقوف','غير نشط'].includes(norm(w.status||w.state))}
  function numericEntityId(row,idx){let raw=S(row.employee_ts_id||row.employee_code||row.entity_id); let m=raw.match(/TS[-_\s]?(\d+)/i); if(m) return Number(m[1]); if(/^\d+$/.test(raw)) return Number(raw); let h=0; for(let i=0;i<raw.length;i++) h=((h*31)+raw.charCodeAt(i))>>>0; return h||idx+1;}
  function clamp(d,m){d=S(d).slice(0,10); if(!d)return''; const a=monthStart(m),b=monthEnd(m); if(d<a)return a; if(d>b)return b; return d;}
  function daysBetween(a,b,m){a=clamp(a,m)||monthStart(m); b=clamp(b,m)||monthEnd(m); if(b<a){const t=a;a=b;b=t} return Math.max(0,Math.round((new Date(b)-new Date(a))/86400000)+1)}
  function savedFor(row,month){const code=S(row.employee_ts_id||row.employee_code); const nid=String(numericEntityId(row,0)); return (st.saved||[]).find(s=>S(s.salary_month).slice(0,7)===month && (S(s.employee_ts_id||s.employee_code)===code || String(s.entity_id)===nid))||{};}
  function attendanceStats(row,month){const keys=[row.employee_ts_id,row.employee_code,row.employee_name,row.residency_name].map(norm).filter(Boolean); const p=new Set(), a=new Set(), allDates=new Set();
    (st.attendance||[]).forEach(r=>{const d=S(r.attendance_date||r.date||r.created_at).slice(0,10); if(!d.startsWith(month))return; const vals=[r.worker_employee_code,r.employee_code,r.employee_ts_id,r.worker_code,r.worker_identity,r.worker_name,r.employee_name].map(norm).filter(Boolean); const ok=keys.some(k=>vals.some(v=>v===k || (k.length>2&&(v.includes(k)||k.includes(v))))); if(!ok)return; allDates.add(d); if(isAbsent(r.status||r.state||r.attendance_status)) a.add(d); else if(isPresent(r.status||r.state||r.attendance_status)) p.add(d);});
    a.forEach(d=>p.delete(d)); return {present:p.size, absent:a.size, dates:[...allDates].sort(), absentDates:[...a].sort()};}
  function calc(row,month){const dim=daysInMonth(month); row.gross_salary=N(row.basic_salary)+N(row.allowance); row.work_days=N(row.work_days); row.absent_days=Math.max(0,Math.min(N(row.absent_days),row.work_days)); row.payable_days=Math.max(0,row.work_days-row.absent_days); row.absence_deduction=Math.round((row.gross_salary/dim*row.absent_days)*100)/100; row.manual_extra_deductions=N(row.manual_extra_deductions||0); row.deductions=Math.round((row.absence_deduction+row.manual_extra_deductions)*100)/100; row.salary_by_days=Math.round((row.gross_salary/dim*row.work_days)*100)/100; const pre=Math.round((row.salary_by_days+N(row.commission)-N(row.deductions)-N(row.advance_deduction))*100)/100; const ce=Math.ceil(pre-0.000001); row.rounding=Math.round(Math.max(0,ce-pre)*100)/100; row.net_salary=Math.round((pre+row.rounding)*100)/100; if(row.absent_days>0&&!row.notes) row.notes='خصم غياب: '+money(row.absent_days)+' '+(row.absent_days===1?'يوم':'أيام'); return row;}
  function makeRow(emp,distRows,month,idx,isSupervisor){const code=codeOf(emp), name=nameOf(emp), sv=savedFor({employee_ts_id:code},month), att=attendanceStats({employee_ts_id:code,employee_name:name,residency_name:resName(emp)},month); const projects=[...new Set((distRows||[]).map(d=>S(d.project_name)).filter(Boolean))].join('، '); const supName=(distRows&&distRows[0])?S(distRows[0].supervisor_name):''; let start=sv.start_date||emp.start_date||emp.work_start_date||emp.joining_date||monthStart(month); let end=sv.end_date||emp.end_date||emp.work_end_date||emp.termination_date||monthEnd(month); start=clamp(start,month)||monthStart(month); end=clamp(end,month)||monthEnd(month); const row={entity_type:'unified',entity_id:code,employee_ts_id:sv.employee_ts_id||code,residency_name:sv.residency_name||resName(emp),employee_name:sv.employee_name||name,iqama_no:sv.iqama_no||iqamaOf(emp),work_location:sv.work_location||(isSupervisor?'FM':(supName||projects||'FM')),project_name:sv.project_name||projects,supervisor_name:isSupervisor?name:supName,job_title:sv.job_title||jobOf(emp),start_date:start,end_date:end,work_days:daysBetween(start,end,month),absent_days:N(sv.absent_days||att.absent),basic_salary:N(sv.basic_salary??basicOf(emp)),allowance:N(sv.allowance??allowanceOf(emp)),commission:N(sv.commission??0),advance_deduction:N(sv.advance_deduction??0),manual_extra_deductions:N(sv.manual_extra_deductions??0),notes:sv.notes||'',row_order:idx}; return calc(row,month)}
  function buildRows(){const month=$('salaryMonth')?.value||today().slice(0,7), type=$('salaryType')?.value||'all', supFilter=S($('salarySupervisor')?.value), projectFilter=S($('salaryProject')?.value), search=norm($('salarySearch')?.value); const byCode=new Map(st.workers.filter(activeWorker).map(w=>[codeOf(w),w])); const dist=(st.dist||[]).filter(d=>S(d.month_key||d.month||'').slice(0,7)===month && !['ended','inactive','deleted'].includes(norm(d.status)));
    const rows=[], groups=new Map(); dist.forEach(d=>{const wc=S(d.worker_employee_code||d.employee_code||d.worker_code); if(!wc)return; if(projectFilter && S(d.project_id)!==projectFilter)return; if(supFilter && S(d.supervisor_employee_code||d.supervisor_code)!==supFilter)return; const sc=S(d.supervisor_employee_code||d.supervisor_code||d.supervisor_id||'بدون'); if(!groups.has(sc)) groups.set(sc,{supervisorCode:sc,supervisorName:S(d.supervisor_name)||sc,workerCodes:new Set(),distRows:[]}); groups.get(sc).workerCodes.add(wc); groups.get(sc).distRows.push(d); });
    let n=0; groups.forEach(g=>{const supEmp=byCode.get(g.supervisorCode)||{employee_code:g.supervisorCode,app_name:g.supervisorName,job_title:'مشرف',basic_salary:2000,allowance:300}; if(type==='all'||type==='supervisors') rows.push(Object.assign(makeRow(supEmp,g.distRows,month,++n,true),{_groupTitle:'المشرف: '+(nameOf(supEmp)||g.supervisorName),_isSupervisor:true})); [...g.workerCodes].forEach(c=>{const emp=byCode.get(c); if(!emp)return; const cat=norm(jobOf(emp)).includes('فني')?'technicians':norm(jobOf(emp)).includes('حارس')?'guards':'workers'; if(type!=='all'&&type!==cat)return; const dr=g.distRows.filter(d=>S(d.worker_employee_code||d.employee_code||d.worker_code)===c); rows.push(makeRow(emp,dr,month,++n,false));}); });
    // أضف الموظفين غير الموزعين فقط عند اختيار الكل وبدون فلتر مشروع/مشرف، حتى لا تضيع رواتب الفنيين والحراس.
    if(!supFilter&&!projectFilter&&(type==='all'||type==='technicians'||type==='guards')){const existing=new Set(rows.map(r=>S(r.employee_ts_id))); st.workers.filter(activeWorker).forEach(emp=>{const cat=norm(jobOf(emp)).includes('فني')?'technicians':norm(jobOf(emp)).includes('حارس')?'guards':norm(jobOf(emp)).includes('مشرف')?'supervisors':'workers'; if(!['technicians','guards'].includes(cat))return; if(type!=='all'&&type!==cat)return; if(existing.has(codeOf(emp)))return; rows.push(makeRow(emp,[],month,++n,cat==='supervisors'));});}
    st.rows=rows.filter(r=>!search||[r.employee_ts_id,r.residency_name,r.employee_name,r.iqama_no,r.work_location,r.project_name,r.supervisor_name,r.job_title].join(' ').toLowerCase().includes(search)); render(); msg('تم بناء الرواتب من النظام الموحد: '+st.rows.length+' سجل');}
  function totals(){return st.rows.reduce((a,r)=>{['basic_salary','allowance','gross_salary','salary_by_days','commission','deductions','advance_deduction','rounding','net_salary'].forEach(k=>a[k]=(a[k]||0)+N(r[k])); return a;},{})}
  function input(r,k,w=110,type='text'){return `<input class="sal-input" style="width:${w}px" value="${esc(r[k]??'')}" oninput="tasneefSalariesUnifiedV435.update('${esc(r.entity_id)}','${k}',this.value)">`}
  function dateInput(r,k){return `<input type="date" class="sal-input" style="width:135px" value="${esc(r[k]||'')}" onchange="tasneefSalariesUnifiedV435.update('${esc(r.entity_id)}','${k}',this.value)">`}
  function rowHtml(r,i,month){return `<tr data-id="${esc(r.entity_id)}"><td>${i}</td><td>${input(r,'employee_ts_id',90)}</td><td>${esc(month)}</td><td>${input(r,'residency_name',175)}</td><td>${input(r,'employee_name',120)}</td><td>${input(r,'iqama_no',120)}</td><td>${input(r,'work_location',140)}</td><td>${input(r,'notes',160)}</td><td>${esc(r.job_title)}</td><td>${dateInput(r,'start_date')}</td><td>${dateInput(r,'end_date')}</td><td>${money(r.work_days)}</td><td>${input(r,'absent_days',70)}</td><td>${money(r.payable_days)}</td><td>${input(r,'basic_salary',85)}</td><td>${input(r,'allowance',75)}</td><td>${money(r.gross_salary)}</td><td>${money(r.salary_by_days)}</td><td>${input(r,'commission',75)}</td><td>${input(r,'manual_extra_deductions',80)}</td><td>${money(r.rounding)}</td><td>${input(r,'advance_deduction',75)}</td><td><b>${money(r.net_salary)}</b></td></tr>`}
  function render(){const body=$('salaryBody'), kp=$('salaryKpis'); if(!body)return; const month=$('salaryMonth')?.value||today().slice(0,7); let html='',i=0,last=''; st.rows.forEach(r=>{if(r._groupTitle&&r._groupTitle!==last){last=r._groupTitle; html+=`<tr class="salary-group-row"><td colspan="23">${esc(last)}</td></tr>`} html+=rowHtml(r,++i,month)}); body.innerHTML=html||'<tr><td colspan="23">لا توجد بيانات رواتب من التوزيع الموحد</td></tr>'; const t=totals(); if(kp)kp.innerHTML=`<div class="kpi"><small>عدد السجلات</small><b>${st.rows.length}</b></div><div class="kpi"><small>الأساسي</small><b>${money(t.basic_salary)}</b></div><div class="kpi"><small>البدلات</small><b>${money(t.allowance)}</b></div><div class="kpi"><small>الخصومات</small><b>${money(t.deductions+t.advance_deduction)}</b></div><div class="kpi"><small>الصافي</small><b>${money(t.net_salary)}</b></div>`; const foot=$('salaryFoot'); if(foot)foot.innerHTML=`<tr><td colspan="14"><b>الإجمالي</b></td><td>${money(t.basic_salary)}</td><td>${money(t.allowance)}</td><td>${money(t.gross_salary)}</td><td>${money(t.salary_by_days)}</td><td>${money(t.commission)}</td><td>${money(t.deductions)}</td><td>${money(t.rounding)}</td><td>${money(t.advance_deduction)}</td><td>${money(t.net_salary)}</td></tr>`;}
  function update(id,k,v){const r=st.rows.find(x=>S(x.entity_id)===S(id)); if(!r)return; const month=$('salaryMonth')?.value||today().slice(0,7); if(['basic_salary','allowance','commission','manual_extra_deductions','advance_deduction','absent_days'].includes(k))r[k]=N(v); else r[k]=v; if(['start_date','end_date'].includes(k)){r.work_days=daysBetween(r.start_date,r.end_date,month)} calc(r,month); render();}
  async function load(){try{if(!window.sb)throw new Error('Supabase غير جاهز'); msg('جاري تحميل الرواتب من النظام الموحد...'); const month=$('salaryMonth')?.value||today().slice(0,7); const start=monthStart(month), end=monthEnd(month); const [emp,wrk,dist,att,projects,saved]=await Promise.all([tryAll('employees_master_v386','*'),tryAll('workers','*'),tryAll('monthly_distribution','*'),tryAll('attendance','*',q=>q.gte('attendance_date',start).lte('attendance_date',end)),tryAll('projects','*'),tryAll('monthly_salaries','*',q=>q.eq('salary_month',start))]); const map=new Map(); [...wrk,...emp].forEach(w=>{const c=codeOf(w); if(c&&!map.has(c))map.set(c,w); else if(c)map.set(c,Object.assign({},map.get(c),w));}); st.workers=[...map.values()]; st.dist=dist; st.attendance=att; st.projects=projects; st.saved=saved; fillFilters(); buildRows();}catch(e){console.error(e); msg('فشل تحميل الرواتب: '+(e.message||e),true)}}
  function fillFilters(){const sup=$('salarySupervisor'), pr=$('salaryProject'); if(sup){const cur=sup.value; const sups=[...new Map((st.dist||[]).map(d=>[S(d.supervisor_employee_code||d.supervisor_code||d.supervisor_id),S(d.supervisor_name||d.supervisor_employee_code||d.supervisor_code||d.supervisor_id)]).filter(x=>x[0])).entries()]; sup.innerHTML='<option value="">كل المشرفين</option>'+sups.map(([c,n])=>`<option value="${esc(c)}">${esc(n)} - ${esc(c)}</option>`).join(''); sup.value=[...sups.map(x=>x[0])].includes(cur)?cur:''} if(pr){const cur=pr.value; const ps=[...new Map((st.dist||[]).map(d=>[S(d.project_id),S(d.project_name||d.project_id)]).filter(x=>x[0])).entries()]; pr.innerHTML='<option value="">كل المشاريع</option>'+ps.map(([c,n])=>`<option value="${esc(c)}">${esc(n)}</option>`).join(''); pr.value=[...ps.map(x=>x[0])].includes(cur)?cur:''}}
  async function save(approve){try{const month=$('salaryMonth')?.value||today().slice(0,7); const rows=st.rows.map((r,i)=>({salary_month:monthStart(month),entity_type:'unified',entity_id:numericEntityId(r,i),employee_ts_id:r.employee_ts_id,employee_code:r.employee_ts_id,residency_name:r.residency_name,iqama_no:r.iqama_no,employee_name:r.employee_name,work_location:r.work_location,project_name:r.project_name,supervisor_name:r.supervisor_name,job_title:r.job_title,start_date:r.start_date||null,end_date:r.end_date||null,work_days:N(r.work_days),absent_days:N(r.absent_days),payable_days:N(r.payable_days),basic_salary:N(r.basic_salary),allowance:N(r.allowance),gross_salary:N(r.gross_salary),salary_by_days:N(r.salary_by_days),commission:N(r.commission),deductions:N(r.deductions),rounding:N(r.rounding),advance_deduction:N(r.advance_deduction),net_salary:N(r.net_salary),notes:r.notes||'',is_approved:!!approve,approved_at:approve?new Date().toISOString():null,updated_at:new Date().toISOString()})); if(!rows.length)return msg('لا توجد رواتب للحفظ',true); const res=await window.sb.from('monthly_salaries').upsert(rows,{onConflict:'salary_month,entity_type,entity_id'}); if(res.error)throw res.error; msg(approve?'تم اعتماد الرواتب':'تم حفظ الرواتب'); await load();}catch(e){console.error(e); msg('فشل حفظ الرواتب: '+(e.message||e),true)}}
  function print(){const old=document.body.innerHTML; const sec=$('salaries'); document.body.innerHTML='<div dir="rtl">'+sec.innerHTML+'</div>'; setTimeout(()=>{window.print(); location.reload();},300)}
  function csv(){const month=$('salaryMonth')?.value||today().slice(0,7); const heads=['أيدي الموظف','الشهر','اسم الموظف في الإقامة','اسم الموظف الحركي','رقم الإقامة','مكان العمل','ملاحظات','الوظيفة','بداية الخدمة','نهاية الخدمة','أيام العمل','أيام الغياب','الأيام المستحقة','الأساسي','البدلات','الإجمالي','راتب الفترة','العمولات','الخصومات','جبر الكسور','السلف','الصافي']; const rows=st.rows.map(r=>[r.employee_ts_id,month,r.residency_name,r.employee_name,r.iqama_no,r.work_location,r.notes,r.job_title,r.start_date,r.end_date,r.work_days,r.absent_days,r.payable_days,r.basic_salary,r.allowance,r.gross_salary,r.salary_by_days,r.commission,r.deductions,r.rounding,r.advance_deduction,r.net_salary]); const data=[heads,...rows].map(r=>r.map(x=>'"'+S(x).replace(/"/g,'""')+'"').join(',')).join('\n'); const a=document.createElement('a'); a.href=URL.createObjectURL(new Blob(['\ufeff'+data],{type:'text/csv'})); a.download='رواتب_'+month+'.csv'; a.click();}
  function inject(){let sec=$('salaries'); if(!sec){const side=document.querySelector('.side'); const ref=[...document.querySelectorAll('.side .nav')].find(b=>S(b.textContent).includes('الأوقات الشهرية')); const btn=document.createElement('button'); btn.className='nav'; btn.textContent='الرواتب'; btn.onclick=function(){showPage('salaries',this); setTimeout(load,60)}; if(side)side.insertBefore(btn,ref?ref.nextSibling:side.querySelector('.nav.danger')); const main=document.querySelector('main.content'); sec=document.createElement('section'); sec.id='salaries'; sec.className='page hidden'; if(main)main.appendChild(sec);} sec.innerHTML=`<style>.salary-table-wrap{max-height:640px;overflow:auto}.salary-table{min-width:2300px}.salary-group-row td{background:#dfeee9;color:#064537;font-weight:900;text-align:right;font-size:13px}.salary-table th{position:sticky;top:0;z-index:2}.sal-input{width:110px;border:1px solid var(--line,#dce6e2);border-radius:8px;padding:6px;text-align:center}.salary-actions{display:flex;gap:8px;flex-wrap:wrap}.salary-note{background:#eef8f4;border:1px solid var(--line,#dce6e2);border-radius:14px;padding:10px;color:#07513f;font-weight:800}</style><div class="card"><div class="table-head"><h2>الرواتب</h2><span class="badge green">${VERSION}</span></div><div class="salary-note">مصدر الرواتب الآن: العمال والبدلات من النظام الموحد، التوزيع من monthly_distribution، الغياب من الحضور والغياب. نفس معادلة كشف الرواتب المعتمدة.</div><div id="salaryMsg" class="msg hidden"></div><div class="filters"><div><label>الشهر</label><input type="month" id="salaryMonth" value="${today().slice(0,7)}" onchange="tasneefSalariesUnifiedV435.load()"></div><div><label>نوع الكشف</label><select id="salaryType" onchange="tasneefSalariesUnifiedV435.buildRows()"><option value="all">الكل</option><option value="supervisors">رواتب المشرفين</option><option value="workers">رواتب العمال</option><option value="technicians">رواتب الفنيين</option><option value="guards">رواتب الحراس</option></select></div><div><label>المشرف</label><select id="salarySupervisor" onchange="tasneefSalariesUnifiedV435.buildRows()"><option value="">كل المشرفين</option></select></div><div><label>المشروع</label><select id="salaryProject" onchange="tasneefSalariesUnifiedV435.buildRows()"><option value="">كل المشاريع</option></select></div><div><label>بحث</label><input id="salarySearch" oninput="tasneefSalariesUnifiedV435.buildRows()" placeholder="اسم/إقامة/TS"></div></div><div class="salary-actions"><button onclick="tasneefSalariesUnifiedV435.load()">تحديث الرواتب</button><button class="light" onclick="tasneefSalariesUnifiedV435.save(false)">حفظ التعديلات</button><button class="light" onclick="tasneefSalariesUnifiedV435.save(true)">اعتماد الرواتب</button><button class="light" onclick="tasneefSalariesUnifiedV435.print()">طباعة</button><button class="light" onclick="tasneefSalariesUnifiedV435.csv()">تصدير CSV</button></div><div id="salaryKpis" class="kpis small"></div><div class="table-wrap salary-table-wrap"><table class="salary-table"><thead><tr><th>رقم</th><th>أيدي الموظف</th><th>الشهر</th><th>اسم الموظف في الإقامة</th><th>اسم الموظف الحركي</th><th>رقم الإقامة</th><th>مكان العمل</th><th>ملاحظات</th><th>الوظيفة</th><th>بداية الخدمة</th><th>نهاية الخدمة</th><th>أيام العمل</th><th>أيام الغياب</th><th>الأيام المستحقة</th><th>قيمة الرواتب الأساسية</th><th>البدلات</th><th>الإجمالي</th><th>إجمالي الراتب على أيام الفترة</th><th>العمولات</th><th>الخصومات</th><th>جبر الكسور</th><th>خصم السلف</th><th>الصافي</th></tr></thead><tbody id="salaryBody"></tbody><tfoot id="salaryFoot"></tfoot></table></div></div>`;}
  window.tasneefSalariesUnifiedV435={inject,load,buildRows,update,save,print,csv}; window.tasneefSalariesV10267=window.tasneefSalariesUnifiedV435; window.tasneefSalariesV10281=window.tasneefSalariesUnifiedV435;
  const oldShow=window.showPage; window.showPage=function(page,btn){const r=oldShow?oldShow.apply(this,arguments):undefined; if(page==='salaries')setTimeout(()=>{inject();load()},80); return r};
  document.addEventListener('DOMContentLoaded',()=>setTimeout(inject,900)); window.addEventListener('load',()=>setTimeout(inject,1100));
})();
