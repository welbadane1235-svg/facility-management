HireX Unique Homepage Form V3

This version intentionally avoids copying the reference app layout.

Differences:
- No square category grid similar to the reference.
- Uses large horizontal professional hub cards.
- Uses a unique hero/search panel.
- Uses compact real metrics strip.
- Uses quick action tiles instead of list rows.
- Professional resume is a premium feature card.

Upload:
index.html
supabase-config.js
